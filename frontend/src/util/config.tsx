import { SurrealistConfig } from "~/typings";

export const BASE_CONFIG: SurrealistConfig = {
	theme: 'automatic',
	tabs: [],
	autoConnect: true,
	tableSuggest: true,
	wordWrap: true,
	queryHistory: [],
	queryFavorites: [],
	localDriver: 'memory',
	localStorage: '',
	enableConsole: true,
	enableListing: false,
	queryTimeout: 10,
	updateChecker: true,
	queryListing: 'history',
}