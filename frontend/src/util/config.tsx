import { SurrealistConfig } from "~/typings";

export const BASE_CONFIG: SurrealistConfig = {
	theme: 'automatic',
	tabs: [],
	autoConnect: true,
	tableSuggest: true,
	wordWrap: true,
	history: [],
	localDriver: 'memory',
	localStorage: '',
	enableConsole: true,
	enableHistory: false,
	queryTimeout: 10,
	updateChecker: true,
}